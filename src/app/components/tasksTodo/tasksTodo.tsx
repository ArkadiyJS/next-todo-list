import React from 'react';
import TaskTodo from '../taskTodo/taskTodo';

function TasksTodo(props) {
  return (
    <ul>
      {/* {props.tasks.map((t)=>(<TaskTodo key={t.id}  title={t.title} completed={t.completed} />))} */}
    </ul>
  );
}

export default TasksTodo;